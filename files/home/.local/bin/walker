#!/usr/bin/env bash

set -euo pipefail

# Walker is a GTK4 app; force the OpenGL renderer only for this process to
# avoid Wayland/Vulkan redraw glitches under Hyprland.
if [[ -z "${GSK_RENDERER:-}" ]]; then
  export GSK_RENDERER=gl
fi

exec /usr/bin/walker "$@"
