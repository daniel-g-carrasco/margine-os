# Arch flavor

`arch` is the default `Margine` flavor.

At the moment it relies entirely on the shared baseline in:

- `manifests/packages`
- `manifests/flatpaks`

Add override manifests here only when `arch` must diverge from the shared
baseline.
