# Baseline AUR packages required to keep the Margine desktop coherent.
#
# Important order:
# - `yay` first, because the AUR is managed through it after bootstrap;
# - `elephant-*` providers before `walker`, because Walker prefixes
#   (`=`, `@`, `$`, `>`) depend on them.

yay
elephant
elephant-calc
elephant-websearch
elephant-windows
elephant-runner
msttcorefonts
walker
