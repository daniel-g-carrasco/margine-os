# Project Status - 2026-04-12

## Topic of the day

Permanently close the refresh of the `Limine + sbctl` trust chain on
real installed path.

## Decisions

- `update-all` now uses writable temporary files for runtime rendering
`limine.conf` and recovery entries, without relying on
  `/usr/local/lib/margine/build`.
- `update-all` reinstalls the unsigned `Limine` binary on the active EFI path
  before `limine enroll-config`.
- `update-all` resigns the active loader immediately after `enroll-config`.
- `refresh-efi-trust` adopts the same sequence, so the manual path and the
  routine maintenance paths stay consistent.

## Verified Operational Status

The following was verified cleanly on the real host system:

- pre-update snapshot created by `update-all`
- `UKI` regeneration completed
- `Config file BLAKE2B successfully enrolled`
- active loader signature `/boot/EFI/Margine/BOOTX64.EFI`
- `sbctl verify` completed cleanly

## Established rule of thumb

The sequence to be considered invariable is:

`deploy -> reinstall unsigned loader -> enroll-config -> sign -> verify`

This rule applies to both:

- `refresh-efi-trust`
- `update-all`
- any other call path that touches the active EFI trust chain