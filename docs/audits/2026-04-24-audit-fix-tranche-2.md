# Margine Public Audit Fix Tranche 2 - 2026-04-24

## Closed Since Tranche 1

- The packaged Walker path is now active on the host.
- `/usr/bin/walker` contains the Margine scroll patch markers:
  `MARGINE_WALKER_SCROLL_STEP` and `scroll_accumulator`.
- The unowned host Walker override at `~/.local/bin/walker` has been removed.
- Host launcher drift has been corrected:
  `check-host-baseline-sync` now returns OK.
- Runtime validation confirms:
  Walker is primary, Fuzzel is fallback, Elephant is active, and the global
  icon theme is `Margine-Adwaita`.

## Point 2 Status: Dirty Worktree Split

The worktree is still intentionally dirty, but the remaining changes now split
into coherent release chunks:

1. Launcher baseline:
   Walker primary, Fuzzel fallback, no Rofi baseline, packaged patched Walker,
   and session-managed Walker/Elephant warm-up.
2. Theme and icon baseline:
   Rewaita V2 GTK colors, no sharp borders, `Margine-Adwaita` icon overlay,
   Adwaita color folders, and MoreWaita folder-name supplementation only where
   Adwaita lacks a folder icon.
3. Runtime validation and update safety:
   actionable `validate-runtime-baseline`, temporary AUR builder sudoers,
   local AUR override staging, English-facing script output touched by this
   tranche, and random-seed permission checks in the root boot validator.
4. Removed/generated cleanup:
   Rofi payload removal, generated bytecode removal, and `__pycache__/` ignore
   coverage.

No commit has been created by this audit tranche. The next clean step is to
commit these as separate changes in the order above, then run VM validation from
those commits.

## Point 3 Status: VM Validation Preparation

The public host runtime baseline is green for the user-level checks available
without root.

Still required before release:

- root boot/recovery validation with `sudo`;
- ESP random-seed permission remediation if the root validator reports it;
- a fresh public VM validation if the public optional gaming stack is intended
  to be release-tested before tagging.

## Verified

- `check-host-baseline-sync`
- `./scripts/validate-runtime-baseline --product margine-public --flavor arch`
- `./scripts/check-shell-and-manifests`
- `git diff --check`
